The following are validation cases compared to EGSnrc.

The output .h5 is not included due to storage considerations, but you can re-run the input files to re-generate the raw data or to tweak parameters. You can then use the script plot_validation.py to examine results.

See $LIONBOLT/examples for more examples.

Note also, 'photons_incident' contains coupled photon-electron transport.
